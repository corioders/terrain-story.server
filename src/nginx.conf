server { 
  gzip on;

  listen 80;

  root /data/www;
  index index.html;


  location = / {
    alias /data/www/index/;
    try_files index.html =404;
  }

  location / {
    try_files $uri $uri/ =404;
  }
}